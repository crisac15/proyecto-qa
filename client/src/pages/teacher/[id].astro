---
import Layout from "@/layouts/MainLayout.astro";
import TeacherCard from "@/components/TeacherCard.tsx";

const { id } = Astro.params;
---

<Layout title="GuíaTEC" currentRoute="profesores">
  <div
    class="absolute inset-0 grid h-screen pt-24 -z-10 lgn:pt-12 place-items-center"
  >
    <TeacherCard teacherID={id ?? ""} client:only="react" />
  </div>
</Layout>
